/**
 * Authentik OIDC integration adapter.
 *
 * Auth.js configuration lives in src/auth.ts.
 * Configure these environment variables for production:
 * - AUTHENTIK_ISSUER
 * - AUTHENTIK_CLIENT_ID
 * - AUTHENTIK_CLIENT_SECRET
 */
export const AUTHENTIK_PROVIDER_ID = "authentik";
